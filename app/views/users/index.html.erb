<h1>Listing users</h1>

<table>
  <tr>
    <th>Name1</th>
    <th>Email1</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
    <td><b><%= link_to 'Show', user %></b></td>
    <td><i><%= link_to 'Edit', edit_user_path(user) %></i></td>
    <td><i><%= link_to 'Destroy', user, confirm: 'Are you sure?', method: :delete %><i></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
